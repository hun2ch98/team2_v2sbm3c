<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.scmenu.ScmenuDAOInter">

  <!-- 평점 글 추가 -->
  <insert id="create" parameterType="dev.mvc.scmenu.ScmenuVO">
    INSERT INTO Scmenu (menuno, name, explan, rdate)
    VALUES (scmenu_seq.nextval, #{name}, #{explan}, sysdate)
  </insert>
  
  <!-- 평점 조회 -->
  <select id="read" parameterType="int" resultType="dev.mvc.scmenu.ScmenuVO">
    SELECT menuno, name, explan, rdate
    FROM scmenu
    WHERE menuno = #{menuno}
  </select>
  
  <!-- 모든 목록 -->
  <select id="list_all" parameterType="dev.mvc.scmenu.ScmenuVO">
    SELECT menuno, name, explan, rdate
    FROM scmenu
    ORDER BY menuno DESC
  </select>
  
  <!-- 등록된 목록 -->
  <select id="list_by_menuno" resultType="dev.mvc.scmenu.ScmenuVO">
    SELECT menuno, name, explan, rdate
    FROM scmenu
    ORDER BY menuno DESC
  </select>
  
  <!-- 내용 수정 -->
  <update id="update_Scmenu" parameterType="dev.mvc.scmenu.ScmenuVO">
    UPDATE scmenu
    SET name=#{name}, explan=#{explan}, rdate=sysdate
    WHERE menuno = #{menuno}
  </update>
  
  <!-- 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM scmenu
    WHERE menuno = #{menuno}
  </delete>
  
  <!-- 검색 목록 -->
  <select id="list_by_menuno_search" resultType="dev.mvc.scmenu.ScmenuVO" parameterType="HashMap">
    SELECT *
    FROM (
        SELECT menuno, name, explan, rdate,
               ROW_NUMBER() OVER (ORDER BY menuno DESC) AS rnum
        FROM scmenu
        WHERE (#{name} IS NULL OR #{name} = '' OR jumsu = #{name})
    )
    WHERE rnum BETWEEN #{startRow} AND #{endRow}
  </select>
  
  <!-- 검색 레코드 갯수 -->
  <select id="count_by_menuno_search" resultType="int" parameterType="HashMap">
      SELECT COUNT(*)
      FROM scmenu
      WHERE (#{name} IS NULL OR #{name} = '' OR jumsu = #{name})
  </select>
   
  <select id="list_search_count" resultType="int">
    SELECT COUNT(*) FROM scmenu
  </select>
  
  <!-- 평점별 검색 + 페이징 목록 -->
  <select id="list_by_menuno_search_paging" resultType="dev.mvc.scmenu.ScmenuVO" parameterType="HashMap">
      SELECT *
      FROM (
          SELECT menuno, name, explan, rdate,
                 ROW_NUMBER() OVER (ORDER BY menuno DESC) AS rnum
          FROM scmenu
          WHERE (#{name} IS NULL OR #{name} = '' OR jumsu = #{name})
      )
      WHERE rnum BETWEEN #{startRow} AND #{endRow}
  </select>

</mapper>
