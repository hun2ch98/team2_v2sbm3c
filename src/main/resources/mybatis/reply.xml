<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.reply.ReplyDAOInter">
  <!-- 등록한 레코드 갯수 1 리턴, #{memberno}: public int getAdminno() 호출됨 -->
  <insert id="create" parameterType="dev.mvc.reply.ReplyVO">
    INSERT INTO reply(replyno, rcontent, rdate, memberno, boardno, passwd)
    VALUES(reply_seq.nextval, #{rcontent}, sysdate, #{memberno}, #{boardno}, #{passwd})
  </insert>

  <!-- 모든 카테고리의 등록된 글목록 -->
  <select id="list_all" resultType="dev.mvc.reply.ReplyVO">
    SELECT replyno, rcontent, rdate, memberno, boardno, fixed_at, passwd, recom
    FROM reply
    ORDER BY replyno DESC
  </select>
  
  <!-- 특정 카테고리의 등록된 글목록 
  <select id="list_by_cateno" resultType="dev.mvc.reply.ReplyVO" parameterType="int">
    SELECT diaryno, memberno, cateno, title, content, recom, cnt, replycnt, rdate,
              LOWER(file1) as file1, file1saved, thumb1, size1, mp4
    FROM contents 
    WHERE cateno = #{cateno}
    ORDER BY diaryno DESC
  </select> -->
  
  <!-- 일반적인 조회 -->
  <select id="read" resultType="dev.mvc.reply.ReplyVO" parameterType="int">
    SELECT replyno, rcontent, rdate, memberno, boardno, fixed_at, passwd, recom
    FROM reply
    WHERE replyno = #{replyno}
  </select>
  
  <!-- 카테고리별 검색 목록 
  <select id="list_by_cateno_search" resultType="dev.mvc.reply.ReplyVO" parameterType="HashMap">
    SELECT diaryno, memberno, cateno, title, content, recom, cnt, replycnt, word, rdate,
            file1, file1saved, thumb1, size1, map, youtube
    FROM contents
    <choose>
      <when test="word == null or word == ''"> 
        WHERE cateno=#{cateno}
      </when>
      <otherwise> 
        WHERE cateno=#{cateno} AND (UPPER(title) LIKE '%' || UPPER(#{word}) || '%' 
                                                  OR UPPER(content) LIKE '%' || UPPER(#{word}) || '%' 
                                                  OR UPPER(word) LIKE '%' || UPPER(#{word}) || '%')
      </otherwise>
    </choose>
    ORDER BY diaryno DESC   
  </select>-->
   
  <!-- 카테고리별 검색 레코드 갯수 
  <select id="list_by_cateno_search_count" resultType="int" parameterType="HashMap">
    SELECT COUNT(*) as cnt
    FROM contents
    <choose>
      <when test="word == null or word == ''"> 
        WHERE cateno=#{cateno}
      </when>
      <otherwise> 
        WHERE cateno=#{cateno} AND (UPPER(title) LIKE '%' || UPPER(#{word}) || '%' 
                                                  OR UPPER(content) LIKE '%' || UPPER(#{word}) || '%' 
                                                  OR UPPER(word) LIKE '%' || UPPER(#{word}) || '%')
      </otherwise>
    </choose>
  </select>   -->

  <!-- 카테고리별 검색 + 페이징 목록 
  <select id="list_by_cateno_search_paging" resultType="dev.mvc.reply.ReplyVO" parameterType="HashMap">
   SELECT diaryno, memberno, cateno, title, content, recom, cnt, replycnt, rdate, file1, file1saved, thumb1, size1, map, youtube, r
   FROM (
              SELECT diaryno, memberno, cateno, title, content, recom, cnt, replycnt, rdate, file1, file1saved, thumb1, size1, map, youtube, rownum as r
              FROM (
                        SELECT diaryno, memberno, cateno, title, content, recom, cnt, replycnt, rdate, file1, file1saved, thumb1, size1, map, youtube
                        FROM contents
                        <choose>
                          <when test="word == null or word == ''">
                            WHERE cateno=#{cateno}
                          </when>
                          <otherwise>
                            WHERE cateno=#{cateno} AND (UPPER(title) LIKE '%' || UPPER(#{word}) || '%' 
                                                              OR UPPER(content) LIKE '%' || UPPER(#{word}) || '%' 
                                                              OR UPPER(word) LIKE '%' || UPPER(#{word}) || '%')
                          </otherwise>
                        </choose>
                        ORDER BY diaryno DESC
               )
    )
    WHERE <![CDATA[ r >= #{start_num} AND r <= #{end_num} ]]>
     
    
  </select>-->
  
  <!-- 패스워드 검사
  <select id="password_check" resultType="int" parameterType="HashMap">
    SELECT COUNT(*) as cnt 
    FROM contents
    WHERE diaryno = #{diaryno} AND passwd=#{passwd}
  </select> -->
  
  <!-- 글 수정 -->
  <update id="update_text" parameterType="dev.mvc.reply.ReplyVO">
    UPDATE contents
    SET title=#{title}, content=#{content},  word=#{word}
    WHERE diaryno = #{diaryno}
  </update>

  <!-- 파일 수정 -->
  <!--<update id="update_file" parameterType="dev.mvc.reply.ReplyVO">
    UPDATE contents
    SET file1=#{file1}, file1saved=#{file1saved}, thumb1=#{thumb1}, size1=#{size1}
    WHERE diaryno = #{diaryno}
  </update>  -->

  <!-- 글 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM contents
    WHERE diaryno=#{diaryno}
  </delete>
  
  <!-- FK cateno 컬럼에 대응하는 필수 SQL -->
  <!-- 특정 카테고리에 속한 레코드 갯수를 리턴 
  <select id="count_by_cateno" resultType="int" parameterType="int">
    SELECT COUNT(*) as cnt 
    FROM contents 
    WHERE cateno=#{cateno}
  </select>-->
  
  <!-- 특정 카테고리에 속한 모든 레코드 삭제
  <delete id="delete_by_cateno" parameterType="int">
    DELETE FROM contents
    WHERE cateno=#{cateno}
  </delete> -->

  <!-- FK memberno 컬럼에 대응하는 필수 SQL -->
  <!-- 특정 카테고리에 속한 레코드 갯수를 리턴
  <select id="count_by_memberno" resultType="int" parameterType="int">
    SELECT COUNT(*) as cnt 
    FROM contents 
    WHERE memberno=#{memberno}
  </select> -->
  
  <!-- 특정 카테고리에 속한 모든 레코드 삭제
  <delete id="delete_by_memberno" parameterType="int">
    DELETE FROM contents
    WHERE memberno=#{memberno}
  </delete> -->

    
</mapper> 

