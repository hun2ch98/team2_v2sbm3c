<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.emotion.EmotionDAOInter">

  <!-- 감정 추가 -->
  <insert id="create" parameterType="dev.mvc.emotion.EmotionVO">
    INSERT INTO emotion (emono, type, explan, img)
    VALUES (emotion_seq.nextval, #{type}, #{explan}, #{img})
  </insert>

  <!-- 감정 조회 -->
  <select id="read" parameterType="int" resultType="dev.mvc.emotion.EmotionVO">
    SELECT emono, type, explan, img
    FROM emotion
    WHERE emono = #{emono}
  </select>

  <select id="list_all" resultType="dev.mvc.emotion.EmotionVO">
    SELECT emono, type, explan, img
    FROM emotion
    ORDER BY emono
  </select>

  <!-- 수정 -->
  <update id="update" parameterType="dev.mvc.emotion.EmotionVO">
    UPDATE emotion
    SET type = #{type}, explan = #{explan},img = #{img}
    WHERE emono = #{emono}
  </update>

  <!-- 감정 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM emotion
    WHERE emono = #{emono}
  </delete>

</mapper>
